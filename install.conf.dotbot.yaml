- defaults:
    link:
        relink: true
        force: true

- clean: ['~']

- link:
    # EDITORS
    ~/.vimrc: vimconfs/vimrc
    ~/.vim: vimconfs/dotvim

    # SHELLS
    ~/.p10k.zsh: shellconfs/zshconfs/p10k.zsh
