#use vim-like keys for splits and windows
bind-key v split-window -h
bind-key s split-window -v
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Mouse Interactivity
setw -g mouse on

# =========== PLUGINS ==================
# ----------PACKAGE MANAGER -----------
# List of plugins (TPM manages the libraries below)
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# ---------- THEMES  -----------------
# set -g @plugin 'arcticicestudio/nord-tmux.git'

# set -g @plugin 'o0th/tmux-nova'
# # one of: true, false; default: false
# set -g @nova-nerdfonts true
# # default: "#S:#I:#W"
# set -g @nova-pane "#I#{?pane_in_mode,  #{pane_mode},}  #W"
# # one of: "left", "right", "centre", "absolute-centre"; default: "left"
# set -g @nova-pane-justify "center"
# set -g @nova-segment-mode "#{?client_prefix,Ω,ω}" # Node segment
# set -g @nova-segment-mode-colors "#50fa7b #282a36"
# set -g @nova-segments-0-left "mode"
# set -g @nova-segment-whoami "#(whoami)@#h" # whoami segment
# set -g @nova-segment-whoami-colors "#50fa7b #282a36"
# set -g @nova-segments-0-right "whoami"

set -g @plugin 'wfxr/tmux-power' 
set -g @tmux_power_theme 'snow'
# # Other colors like gold, redwine, moon, forest, violet, snow, coral, 'sky'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# Below line to install without pulling things
run '~/.tmux/plugins/tpm/tpm'
